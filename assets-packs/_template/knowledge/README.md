# knowledge/（spec §10）

包内知识条目（`*.md`），由 `manifest.json` 的 `knowledge` 数组逐条引用。

- 一文件一主题，面向目标年龄的表述（无适龄争议内容走 T18 审核后再入库）。
- 知识仅限包内使用（包隔离，T16-G1-01）；不得写入训练集或跨包引用。
- 本 README 为 _template 占位说明：建新包时替换为真实知识条目并同步更新 manifest。
