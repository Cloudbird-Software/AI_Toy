[project]
name = "holdout"
version = "0.0.1"
description = "ai-toy tool: holdout"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
holdout = "holdoutctl.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/holdoutctl"]
