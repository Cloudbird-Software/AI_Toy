[project]
name = "budgets"
version = "0.0.1"
description = "ai-toy tool: budgets"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
budgets = "budgets.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/budgets"]
