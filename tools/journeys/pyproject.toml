[project]
name = "journeys"
version = "0.0.1"
description = "ai-toy tool: journeys"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
journeys = "journeys.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/journeys"]
