[project]
name = "journeys"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["pyyaml>=6"]

[project.scripts]
journeys = "journeys.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/journeys"]
