[project]
name = "gaterunner"
version = "0.0.1"
description = "ai-toy tool: gaterunner"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
gaterunner = "gaterunner.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/gaterunner"]
