# AGENTS.md — 情绪引擎（T7）
验收协议：docs/gates/assets/T7.md（先读，BI 编号以它为准）　阈值：configs/gates/T7.yaml（禁改）
## 本包边界
情绪检测+状态动力学+恢复：对话文本/语音特征与事件流进 → 情绪状态（愉悦度×唤醒度+亲密度）出。对接 T9（情绪不越界联跑）、T12（情绪→动作映射消费方）、T8（人格基线）。
## 技术路径（指导，任选+可偏离，PR 记录选择）
A 评价理论（OCC/Scherer）显式规则+低维动力学（愉悦度×唤醒度+亲密度，衰减演化；全符号可断言，默认路线）｜B 学习型检测（端侧 wav2vec2/HuBERT 情感头或复用 T5 骨干）+积分器（GoEmotions 27 类收敛到儿童 8–10 类，需 A 兜底）｜C LLM 内隐情绪（prompt 内维护情绪段；不可断言/随版本漂移，不作主路线，仅作对照基线）
## 本地命令
just gate T7 all ；go test ./packages/go/emotion -run Property -count=1
## 本地必绿再提 PR
T7-G0-01 情绪不越界：全情绪扫描×对抗场景与 T9 联跑，伤人话/恐吓/尖叫级 0 次｜T7-G1-01 方向一致≥85%（≥300 情绪事件）｜T7-G1-02 状态连续性 单轮跳变≤0.3｜T7-G1-03 可恢复性 ≤30min 回基线、无吸收态｜T7-G1-04 检测延迟 P95≤900ms
## 数据依赖
datasets/manifests/emotion_synth.json（synthgen 注册：300 情绪事件合成集，10 类×30 例）；真实儿童哭闹/撒娇/赌气 ≥50 段（holdout，只进不出，经 tools/holdout，本包代码不得直接读）
## 本包禁令（叠加根 AGENTS.md）
- 状态实现必须纯函数可复现（同序列同初始态同终态），随机只许在表达层
- 无关事件（日志/心跳/噪声）不得改状态；所有维度 ∈[0,1] 永不 NaN（属性级）
## 常见坑
「情绪理解」宣称须合成 ≥85% 且真实 ≥75% 两数分开报；无输入时快变量必须单调回归基线（李雅普诺夫式：到基线距离随静置单调不增），别把衰减写成常数
