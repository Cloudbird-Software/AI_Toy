# AGENTS.md — 声纹与家庭成员识别（T5）
验收协议：docs/gates/assets/T5.md（先读，BI 编号以它为准）　阈值：configs/gates/T5.yaml（禁改）
## 本包边界
家庭成员声纹注册/闭集验证/拒判事件：特征进 → UserID 绑定话轮出；拒判 → T10 记忆只读（CI-2/CH-05 联动，事件由 loop 搬运，包间零 import）。对接 T3（话轮身份绑定）、T10（拒判→只读 G0 联跑）、T14（端侧档位）。真模型 SV 引擎后续落位 packages/go/speaker 骨架（法典卡面），M3 不动不删。
## 实现状态（M3，IR #106）
规则面已实现（注册/闭集最近邻验证/拒判事件/EER 评估通道，m3-spec §5 包契约 D）：内置余弦打分器=合成特征通道桩（无真实声学语义，真模型 ECAPA-TDNN 接入后同协议 L5 复测）；G1-02 debt（隔天/换房跨会话真实数据面未建）；T5-G0-01×T10-G0-01 联跑=CI-2 语义桩先行（#105 真身合并后 loop 组装面复测）；门禁报告 reports/gates/T5.json。
## 技术路径（指导，任选+可偏离，PR 记录选择）
A SpeechBrain ECAPA-TDNN（Apache-2.0，VoxCeleb EER<1% recipe，ONNX 端侧，默认）｜B 3D-Speaker/CAM++（中文生态，与 A 同协议横评后自选）｜C 共享骨干嵌入+家庭内 N≤8 闭集轻量头（算力受限时更稳）
## 本地命令
just gate T5 all ；go test ./packages/go/voiceprint -run Property -count=1
## 本地必绿再提 PR
T5-G0-01 身份切换 0 泄漏（≥100「写入 A→拒判/切 B→询问 B」场景，与 T10 联跑）｜T5-G1-01 家庭内区分 EER≤0.05（兄弟姐妹对单列）｜T5-G1-02 跨会话再识别 ≥95%、拒判 ≤3%｜T5-G1-03 3 句注册 EER 劣化 ≤0.02｜T5-G1-04 陌生人拒判 ≥0.90
## 数据依赖
合成虚拟家庭（生成器/打分器解耦+参数冻结，防「生成器迎合打分器」——m3-spec §5）；真实跨会话/家庭录音=holdout 侧（亲友家庭 ≥5 家×3–6 人×3 会话，知情同意，经 tools/holdout，本包代码不得直接读；T2 数据飞轮供数）
## 本包禁令（叠加根 AGENTS.md）
- 克隆真实儿童声音为红线（与 T13 同源）；真实/合成一律分开两列报告，禁止合并
- EER/拒判统计一律走 evalkit（trial 数+miss/FA 全报），勿手算；零失败宣称按 n≥59/149/299 纪律
- 拒判不得冒认任何注册身份（unknown 归属零冒认，CI-2 禁半绑定）；打分器注入面只供测试，产品路径不得绕过 Verify 闭集门
## 常见坑
兄弟姐妹近距簇是 EER 重灾区（单列报告不并入总体）；公开基准 EER 不可迁移（成人/朗读/棚录分布），一切阈值以自建「家庭内区分」协议为准
