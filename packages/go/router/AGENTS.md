# AGENTS.md — 路由与缓存（T15）
验收协议：docs/gates/assets/T15.md（先读，BI 编号以它为准）　阈值：configs/gates/T15.yaml（禁改）
## 本包边界
端云分发与语义缓存：query+上下文进 → 路由决策（端/云中档/云旗舰/缓存命中）出。对接 T14（端侧能力上界）、T9（安全 query 永过完整防线）、T2（学习型路由=数据飞轮第一个变现点）。
## 技术路径（指导，任选+可偏离，PR 记录选择）
A 语义缓存自建轻量版（GPTCache 思路；缓存键带角色/用户/情绪上下文，默认）｜B 级联路由（FrugalGPT：端侧→云中档→云旗舰，规模化主策略）｜C 学习型路由（RouteLLM：真实 query 日志积累后替换 B 的规则阈值）
## 本地命令
just gate T15 all ；go test ./packages/go/router -run Property -count=1
## 本地必绿再提 PR
T15-G0-01 缓存误命中：对抗对 200 组误命中=0（自然流≤0.5%；安全类 query 永不缓存命中）｜T15-G1-01 路由正确率≥92%（安全敏感类路由错误=0，升 G0 行为）｜T15-G1-02 命中≥30%、成本较纯云降≥40%（30 天仿真流×3 档）｜T15-G1-03 路由决策 P95≤30ms
## 数据依赖
datasets/manifests/router_synth.json（synthgen 注册：对抗对 200 组+30 天仿真流+意图分层标注 500 条）；种子家庭真实 query 分布（脱敏 holdout，重测用，经 tools/holdout，本包代码不得直接读）
## 本包禁令（叠加根 AGENTS.md）
- θ（缓存相似度阈值）必须先跑 θ-命中/θ-误命双曲线、G0 对抗约束定 θ 上界后再选点
- 语气词改写（嘛/呀/啦）不得改变路由目标（属性级）
## 常见坑
孩子的问法与成人完全不同，仿真分布下调好的路由在真实 holdout 上常失效——发布前必跑脱敏真实分布；同 query+同缓存态+同阈值须同决策（消抖动）
