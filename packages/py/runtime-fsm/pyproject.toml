[project]
name = "toy-runtime-fsm"
version = "0.0.1"
description = "ai-toy package: runtime-fsm (T14 离线运行时)"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
test = ["pytest", "hypothesis", "evalkit", "gaterunner"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/runtime_fsm"]
