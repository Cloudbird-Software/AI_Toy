[project]
name = "toy-motion-map"
version = "0.0.1"
description = "ai-toy package: motion-map (T12 情绪动作映射)"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
test = ["pytest", "hypothesis", "evalkit", "gaterunner"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/motion_map"]
