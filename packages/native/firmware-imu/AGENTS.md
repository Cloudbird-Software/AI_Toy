# AGENTS.md — IMU 固件（T6 固件，C++）
验收协议：docs/gates/assets/T6.md（先读，BI 编号以它为准；与 packages/go/imu 共担 T6）
阈值：configs/gates/T6.yaml（禁改）
## 本包边界
MCU 固件层：IMU 原始读数与上层动作指令进 → 电机占空比/角度硬件熔断与摔落停机执行出（感知决策在 packages/go/imu）。对接 T6（与 imu 共担）、T14（待机功耗/档位联动）、T12（动作映射安全盒的硬件下界）。
## 技术路径（指导，任选+可偏离，PR 记录选择）
A 经典滑窗特征+决策树/小 SVM（50Hz，零框架依赖，可打印审计）｜B Google Personify（Apache-2.0，端侧 IMU 个人化异常检测）｜C TFLite Micro 1D-CNN（细粒度动作增强层，不做底座）；熔断不走学习路线：输入冲击等级×输出动作矩阵表驱动穷举（见验收协议）
## 本地命令
just gate T6 all ；表驱动穷举（冲击等级×输出动作矩阵）与看门狗测试随固件构建执行（C++ 构建入口落地后在此补齐——本包不在 cargo workspace，cargo nextest 不适用）
## 本地必绿再提 PR
T6-G0-03 电机占空比/角度硬件熔断（表驱动穷举，≤数据表安全值，violation=0）｜T6-G0-02 摔落/抛掷保护端侧执行（检出≥95%，≤2s 停马达静音）｜看门狗（喂狗路径阻塞注入穷举）；同资产 Go 侧断言（T6-G0-01 静置 0 自发输出、T6-G1-01 拿起≥98%）见 packages/go/imu
## 数据依赖
台架脚本（跌落台架 ≥30 次 1m 落地毯/木地板；持续输出 30min；真机 12h 夜间挂机 ×3 台）；真机 3 台为 holdout（只进不出），一律经 tools/holdout，本包代码不得直接读
## 本包禁令（叠加根 AGENTS.md）
- 熔断逻辑独立于应用层，任何软件 bug 不可越过
- 熔断参数上界=数据表安全值，不得为调试临时放宽
- 不得以软件层双保险替代固件层熔断（软件双保险不是唯一保险）
## 常见坑
马达边界必须在固件层——应用层崩溃/死循环时熔断仍须生效；看门狗喂狗不得被主循环阻塞掩盖（喂狗路径单独断言）；T6-G1-02 待机功耗阈值规格书未落盘（configs/gates/T6.yaml 头注），别拿未定数值当门禁
